# TCH-02 manual test

Use a teacher account on development (or teacher preview shell with fake data).

1. Open **Classes**. Confirm active assignment cards only.
2. Open an assignment. Confirm roster (names) or empty state — not an error if no enrollments.
3. From Dashboard, tap an assignment card — opens that roster via `/classes?assignment=…`.
4. Attempt an unknown assignment id (or leave/end assignment) — access denied / error, no other class roster.

Reply **NEXT** to approve, or **FIX:** with defects.
