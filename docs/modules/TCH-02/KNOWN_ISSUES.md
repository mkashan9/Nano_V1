# TCH-02 known issues

- Development seed may have assignments without linked `class_id`/enrollments → empty roster until SCH structure + enrollments exist.
- Section filtering on roster is deferred until SCH-04 enrollments write `section_id`.
