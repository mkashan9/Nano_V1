# CLS-03 manual test

Use a teacher account (or preview shell with fake data).

1. Open **Classroom**, create a draft with **Require acknowledgement**.
2. Set **Schedule publish** to a future date → Save → list shows `scheduled`.
3. Create another item with **Publish now** → list shows `0/N acknowledged`.
4. Edit a draft and set **Expires on**; clear schedule/expiry works.

Reply **NEXT** to approve, or **FIX:** with defects.
