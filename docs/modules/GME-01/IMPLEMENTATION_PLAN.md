# GME-01 implementation plan

1. Add `independent_allowed` + school-only seed + learner list RPC.
2. Domain/data catalog models and fake/supabase repos.
3. Replace Games placeholder with catalog page; wire live repo in `main`.
4. Docs + tests; stop at USER_TEST.
