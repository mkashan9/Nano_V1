# COM-04 manual test

1. As a senior member, open Communities → a community you belong to →
   **Open chat**.
2. Send a text message. Confirm it appears in the thread.
3. Tap **Reply** on a message, send; confirm reply context shows.
4. Tap `@` (mention), pick a member, send; confirm send succeeds.
5. Tap **React**, choose an emoji; toggle again to remove.
6. Non-members must not see Open chat until they join.
7. Restricted words / flood should fail server-side (SAFE-03).
