# ADM-06 implementation plan

1. Migration: `games` / `game_versions`, platform RLS select, admin RPCs, seed.
2. Domain + Fake/Supabase repositories.
3. Admin Games hub wired to nav (`platform.games`).
4. Unit/widget/SQL smoke tests and module docs → USER_TEST.
