# VIS-07 known issues

- Flutter golden/`toImage` capture is unreliable on Windows; layout stand-in is primary compare input (`REVIEW_REQUIRED`).
- Logic Factory / Business Empire tiles use icon fallbacks until more art is accepted.
- Challenges and unlock worlds are visual fixtures (not live tournament backends).
