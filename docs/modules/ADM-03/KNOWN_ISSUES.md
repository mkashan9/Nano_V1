# ADM-03 known issues

- Replace-admin dialog takes a raw user UUID (preview defaults to the teacher
  fixture). Email lookup stays deferred with password-reset mail.
- Search is capped at 50 rows; pagination is not in this module.
