# ADM-07 implementation plan

1. Migration: `notification_templates`, platform RLS select, admin RPCs, seed.
2. Domain + Fake/Supabase repositories.
3. Admin Notifications hub wired to nav (`platform.notifications`).
4. Unit/widget/SQL smoke tests and module docs → USER_TEST.
