# SAFE-04 known issues

- Onboarding completion does not re-call `my_community_entitlements` live;
  after a platform kill-switch flip, learners may need a re-sign-in.
- COM-01 still owns create/discover UX; this module only entitles seniors.
