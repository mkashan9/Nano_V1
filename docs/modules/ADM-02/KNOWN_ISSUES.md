# ADM-02 known issues

- Assign admin takes a raw user UUID (preview defaults to the Alpha school
  admin fixture). Email lookup waits on ADM-03 user search.
- Platform dashboard school directory is still read-only; live counts refresh
  after Schools mutations on next Platform load.
