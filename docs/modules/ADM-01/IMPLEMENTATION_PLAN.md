# ADM-01 implementation plan

1. Domain `PlatformDashboard` + privacy helpers.
2. `platform_dashboard` RPC (platform admin only).
3. Repository fake + Supabase; wire from `admin_web` main.
4. Replace Platform stub with `PlatformDashboardPage`.
5. Tests + docs; stop at USER_TEST.
