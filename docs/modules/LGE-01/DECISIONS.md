# LGE-01 decisions

- Score = sum of `xp_ledger` rows with `source_kind = game_result` in the open week.
- Default division on join is Bronze; promotion/relegation deferred.
- Peer identities are not exposed; only personal rank + peer_count.
- Independent learners pool separately (`school_id` null).
