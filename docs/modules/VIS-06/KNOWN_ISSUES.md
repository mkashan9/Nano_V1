# VIS-06 known issues

- Flutter golden/`toImage` capture is unreliable on Windows; layout stand-in is primary compare input (`REVIEW_REQUIRED`).
- Some category tiles use icon fallbacks (Business / History / Design) until more generated art is accepted.
- Mentor chat is not wired to a live conversation surface yet.
