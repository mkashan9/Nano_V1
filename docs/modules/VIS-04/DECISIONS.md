# VIS-04 decisions

- Junior Profile is a dedicated visual page; senior/list Me (`StudentProfilePage`) stays for non-junior.
- Level / XP bar values match the reference fixtures (Level 7, 320/500) for visual parity; live ledger remains STU-05 / XP modules.
- Sound toggles `accessibility.soundEnabled`; Language cycles EN/UR; Dark Mode is local UI state pending a theme preference.
- Recent Learning and Weekly Journey use VIS-04 fixtures until progress APIs feed kids tiles.
