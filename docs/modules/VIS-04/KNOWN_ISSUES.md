# VIS-04 known issues

- Flutter golden/`toImage` capture is unreliable on Windows; layout stand-in is the primary compare input (`REVIEW_REQUIRED` vs photo ref).
- Dark Mode toggle does not yet change app theme.
- For Parents opens Parent Guidance when a repository is wired; otherwise shows a snackbar.
