# NOT-01 known issues

- No live device-token table or provider delivery.
- Simulate push is a demo control on the inbox app bar.
- OS lock-screen rendering is policy-only (string preview), not a system UI.
