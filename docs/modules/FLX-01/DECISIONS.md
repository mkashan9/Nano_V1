# FLX-01 decisions

- Hub-first: three cards; section detail deferred to FLX-02/03/04 placeholders.
- Eligibility gated by `SessionPrincipal.flexEligible` + nav catalog.
- Deep links like `/flex/marks` resolve to `/flex` when eligible.
- Fake repository before live academic read models.
