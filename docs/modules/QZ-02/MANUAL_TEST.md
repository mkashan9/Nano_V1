# QZ-02 manual test

Run `admin_web` as Superadmin (shell switcher → Superadmin if previewing).

1. Open **Content**. Confirm tabs for **Question bank** and **Topic quizzes**.
2. Switch to **Topic quizzes**. Confirm Counting, Addition, and Living things
   quizzes show as **Published** with ordered questions (Question 1…).
3. Tap **New quiz**. Confirm a draft appears (Ecosystems) with at least one
   ordered item.
4. Tap **Publish quiz**. Confirm status becomes **Published**.
5. Confirm **Junior preview** and **Senior preview** both show the same version
   id.
6. Switch the shell to **School**. Open Content — it should stay a placeholder
   without quiz authoring.

Reply `NEXT` to approve, or `FIX: …` if authorship, order, or preview looks wrong.
