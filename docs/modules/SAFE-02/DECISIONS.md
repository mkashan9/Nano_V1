# SAFE-02 decisions

- Reuse ADM-03 `set_profile_status` for suspend; never expose reported user id to clients.
- Claiming a report marks `under_review` and logs evidence access.
- Resolution note is required for every close action.
- Media review stays MED-05; hub tabs share the Moderation nav destination.
