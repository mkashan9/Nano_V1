# ATT-01 known issues

- Development seed may have empty class rosters → empty grid until enrollments exist.
- Session-mode period UI is stubbed (policy may be `session`; grid still uses `daily` when mode is daily).
- Durable local draft queue not wired into teacher_app yet.
