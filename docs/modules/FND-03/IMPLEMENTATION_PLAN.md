# FND-03 Implementation Plan

## Scope

- `NanoResponsive` / `NanoResponsiveBuilder` / `NanoMaxContentWidth`
- Shared `LearningSubject` domain + student home fixtures
- `JuniorHomeFoundation` and `SeniorHomeFoundation`
- Text-scale smoke test + subject parity test

## Out of scope

- Real bottom navigation destinations (FND-04)
- Auth / experience resolution from profile (AUTH/STU)
