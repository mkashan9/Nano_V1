# GME-07 implementation plan

1. Extend `disable_game_version` to abort active sessions.
2. Add `get_game_session_play_status` for host polling.
3. Map NS143 / kill poll to learner copy on `GameHostPage`.
4. Document + tests; stop at USER_TEST.
