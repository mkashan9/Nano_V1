# VIS-09 known issues

- Flutter golden/`toImage` capture is unreliable on Windows; layout stand-in is primary compare input (`REVIEW_REQUIRED`).
- Teams and clubs are fixture cards (not live discovery lists).
- Challenge join is a snackbar placeholder until challenge modules land.
