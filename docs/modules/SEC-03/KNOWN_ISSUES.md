# SEC-03 Known Issues

- No auth.users FK yet; session revocation UI waits on AUTH modules.
- Privileged mutation audit writers (edge/RPC) deferred to AUTH / ADM modules.
- CI workflow still pending PAT `workflow` scope.
