# QA-01 known issues

- Checklist uses a safe preview EnvironmentConfig in the shell (not live dart
  defines from the running web build).
- Does not replace remote Supabase advisor review or pen-testing.
- Secret pattern list is heuristic, not exhaustive.
