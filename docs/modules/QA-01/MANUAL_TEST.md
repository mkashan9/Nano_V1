# QA-01 manual test

1. Open **admin_web** as superadmin.
2. Open **Security** in the side rail (`/audit`).
3. Confirm **All checks passed** and AccessGuard / anon-only rows are green.
4. (Optional) Confirm domain test rejects anon keys containing `service_role`.
