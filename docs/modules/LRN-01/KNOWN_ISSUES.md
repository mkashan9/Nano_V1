# LRN-01 Known Issues

- Home fixtures still list English/Stories for visual richness; those IDs are not in the catalog yet, so a tap shows the empty state until content is curated.
- Topic Start/Resume is a callback stub — playback belongs to LRN-03.
- Superadmin authoring UI is out of scope; seeds and service-role writes cover development.
- `authenticated` can execute eligibility helpers (security definer, read-only) — intentional for RLS.
