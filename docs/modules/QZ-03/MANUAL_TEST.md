# QZ-03 manual test

Run `student_app` as Junior (shell switcher → Junior if previewing).

1. Open Learning and enter **Counting** (or any unlocked topic with a quiz).
2. Tap **Take quiz**. Confirm one question fills the screen with Nori and
   large answer choices — no “correct answer” label.
3. Pick an answer, tap **Next**, finish the last question with **Finish**.
4. Confirm the done screen says the score will be saved later (no percent).
5. Switch the shell to **Senior**. Open the same topic — there should be no
   Junior **Take quiz** entry into this one-question UI.

Reply `NEXT` to approve, or `FIX: …` if the flow or companion prompts look wrong.
