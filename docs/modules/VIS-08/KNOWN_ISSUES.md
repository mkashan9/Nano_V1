# VIS-08 known issues

- Flutter golden/`toImage` capture is unreliable on Windows; layout stand-in is primary compare input (`REVIEW_REQUIRED`).
- Friends stack uses placeholder avatars (not live friend graph).
- Hex achievements are rectangular tiles approximating the reference hex badges.
- STU-05 privacy/devices settings are not on the visual Me surface (use legacy flag or Settings entry).
