# VIS-08 decisions

- Senior Profile visual layout is default via `StudentProfileTab.useVisualLayout` (true).
- STU-05 list Me (privacy, devices, social) remains available with `useVisualLayout: false`.
- Metrics / goals / achievements / builders / journey are visual fixtures until XP/social backends wire in.
- Reuses `SeniorLearningPathsPanel` and `SeniorDashboardStatCard` from VIS-05/06.
