# QZ-05 manual test

Run `student_app` as Junior.

1. Open Counting → **Take quiz**. Answer question 1, leave the app screen
   (pop), reopen **Take quiz**. Confirm the prior answer is still selected
   (resume).
2. Finish the quiz. Confirm a **Score: …%** line and
   “This score was saved by the server.” appear.
3. Optional: trigger submit twice in tests / RPC to confirm the same percent.
4. Switch to Senior, finish via **Review & finish**, confirm the same server
   score path (not a client-invented percent before submit).

Reply `NEXT` to approve, or `FIX: …` if resume or scoring looks wrong.
