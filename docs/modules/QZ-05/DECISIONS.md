# QZ-05 decisions

- Score only on `submit_quiz_attempt`; fakes mirror that boundary for UI tests.
- `max_retakes` counts submitted attempts already finished (null = unlimited).
- Progress/XP updates wait for QZ-06 / XP-01.
