# VIS-05 known issues

- Flutter golden/`toImage` capture is unreliable on Windows; layout stand-in is the primary compare input (`REVIEW_REQUIRED` vs photo ref).
- Builder Dashboard project/hours/problems stats are visual fixtures (XP uses live summary when available).
- Challenge countdown is static fixture text.
