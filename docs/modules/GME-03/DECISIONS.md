# GME-03 decisions

- Reuse GME-02 sessions, tokens, and bridge; do not fork the protocol.
- Flutter fixtures use `fixture://` origins (same allowlist helper).
- Shape Sort is a Nano first-party native reference (not a third-party
  binary). Future OSS titles need license rows before production ship.
- Client completion remains unverified (GME-05).
- Tap-to-sort UX (no drag) for junior accessibility and reliable tests.
