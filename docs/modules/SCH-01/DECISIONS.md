# SCH-01 decisions

## Split from ADM-02

Platform create/code/status/first-admin stay ADM-02. SCH-01 is school-scoped
read + branding write for the signed-in school admin.

## New repository name

`SchoolDashboardRepository` avoids colliding with ADM-02’s
`SchoolAdminRepository` (platform school ops).

## Brand slots only

Primary/secondary hex colors only. Safety colors remain in the design system.
Logo is a URL string for now (no storage upload).
