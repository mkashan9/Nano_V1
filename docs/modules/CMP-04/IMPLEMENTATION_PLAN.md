# CMP-04 Implementation Plan

1. Protect VIS-09 dirty tree; work only in `nano-cmp04-worktree`.
2. Preserve master; cutout; static pose pack; provenance.
3. `CompanionPosePack` + deprecate purple `NoriPosePack` assets.
4. Male voice default migration; narration script refresh.
5. Event/cooldown/placement wiring; Junior profile portrait.
6. Tier-2 clips via Wan when feasible, else static + `VIDEO_REVIEW_REQUIRED`.
7. Dev gallery `/dev/companion-cmp04`; tests; screenshots; USER_TEST.
