# CMP-04 Decisions

- Keep `NoriPosePack` name as deprecated forwarder for compatibility.
- Do not wire per-question `quizCorrect`/`quizWrong` (learner flow hides correctness until results).
- Historical narration voice rows retained; `aoede` disabled for new defaults.
