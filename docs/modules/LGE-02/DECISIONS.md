# LGE-02 decisions

- Pool = same division + school_id (or independent) as LGE-01 ranks.
- Labels via first token of `profiles.display_name`; `discoverable=false` → Learner.
- No client select on snapshots; RPC only.
- Soft junior copy; no rank-loss messaging.
