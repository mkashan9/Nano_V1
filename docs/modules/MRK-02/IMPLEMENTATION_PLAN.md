# MRK-02 implementation plan

1. Migration: `marks_entries` + load/save RPCs; dashboard unpublished count.
2. Domain + repository methods on assessment repository.
3. Marks page grid UI (Enter marks → save).
4. Tests + docs → USER_TEST.
