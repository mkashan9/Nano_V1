# SCH-06 decisions

- Policies live under Settings as a second tab beside Branding (SCH-01).
- One `school_marks_policies` row per school; grade bands stored as JSON.
- Result periods close with audited reason; teacher correction workflow stays MRK.
- Attendance mode is policy only — entry UI stays ATT modules.
