# SOC-01 implementation plan

1. Migrate `social_identities` + RPCs; update `league_privacy_label`.
2. Domain `SocialIdentity` / `LimitedProfile` + fake/live repositories.
3. Profile UI: claim username, copy/rotate code, lookup limited profile.
4. Tests + MANUAL_TEST.
