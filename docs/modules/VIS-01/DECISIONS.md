# VIS-01 — Decisions

1. **Visual layout is default Junior Home.** Missions/companion are opt-in flags.
2. **Screenshot routes bypass auth.** `/screenshot/*` never in production nav.
3. **Assets via Cursor GenerateImage** with provenance YAML; no provider keys in Flutter.
4. **Layout stand-in for compare on hung golden hosts.** Widget tests remain the functional gate.
5. **Star badge uses streakDays** from fixtures (7).
