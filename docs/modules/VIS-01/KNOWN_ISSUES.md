# VIS-01 known issues

- `matchesGoldenFile` / `toImage` hang at phone sizes on the agent Windows host.
- Layout stand-in SSIM vs photographic reference is low by design — not a Flutter raster.
- English/Stories generated art may contain residual glyphs → ASSET_REVIEW_REQUIRED.
- Illustration-on-device capture with assets still pending a non-hanging capture path.
