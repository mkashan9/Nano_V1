name: nano_testing
description: Nano shared test fixtures and helpers
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.2

resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  nano_domain:
    path: ../nano_domain

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
