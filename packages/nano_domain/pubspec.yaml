name: nano_domain
description: Nano shared domain models and environment contracts
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.2

resolution: workspace
dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
